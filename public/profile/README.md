# Portfolio profile assets

Replace `profile.jpg` in this folder whenever the portfolio profile photo changes. Keep the filename and extension unchanged so the site updates automatically.

`m2p-logo.svg` is the stable M2P brand asset. Do not replace it when updating the profile photo.
